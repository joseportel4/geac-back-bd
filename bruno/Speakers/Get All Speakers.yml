info:
  name: Get All Speakers
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/speakers"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
