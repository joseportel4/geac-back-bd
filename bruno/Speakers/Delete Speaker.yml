info:
  name: Delete Speaker
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/speakers/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
