info:
  name: Delete Category
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/categories/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
