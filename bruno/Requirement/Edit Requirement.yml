info:
  name: Edit Requirement
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{baseUrl}}/requirements/1"
  body:
    type: json
    data: |-
      {
          "description": "Fazer testes"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
