info:
  name: Get Requirement by ID
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/requirements/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
