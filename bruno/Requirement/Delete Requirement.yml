info:
  name: Delete Requirement
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/requirements/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
