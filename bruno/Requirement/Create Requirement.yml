info:
  name: Create Requirement
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/requirements"
  body:
    type: json
    data: |-
      {
          "description": "Fazer testes"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
