info:
  name: Get All Requirements
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/requirements"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
