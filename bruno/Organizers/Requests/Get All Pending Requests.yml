info:
  name: Get All Pending Requests
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/organizer-requests/pending"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
