info:
  name: Reject Request (Admin Only)
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/organizer-requests/1/reject"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
