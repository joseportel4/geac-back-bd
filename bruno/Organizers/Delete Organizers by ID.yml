info:
  name: Delete Organizers by ID
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/organizers/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
