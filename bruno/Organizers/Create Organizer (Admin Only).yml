info:
  name: Create Organizer (Admin Only)
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/organizers"
  body:
    type: json
    data: |-
      {
        "name": "Departamento de Física",
        "contactEmail": "depfisica@gmail.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
