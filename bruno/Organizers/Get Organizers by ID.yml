info:
  name: Get Organizers by ID
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/organizers/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
