info:
  name: Get All Organizers
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/organizers"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
