info:
  name: Get All Events
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/events"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
