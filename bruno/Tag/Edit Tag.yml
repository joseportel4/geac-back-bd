info:
  name: Edit Tag
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{baseUrl}}/tags/1"
  body:
    type: json
    data: |-
      {
        "name": "Testes"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
