info:
  name: Create Tag
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/tags"
  body:
    type: json
    data: |-
      {
        "name": "Testes"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
