info:
  name: Get Location by ID
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/locations/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
